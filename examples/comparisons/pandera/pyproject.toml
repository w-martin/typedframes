[project]
name = "pandera-comparison"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.11"
dependencies = [
    "pandas>=3.0.1",
    "pandera[mypy]>=0.29.0",
]

[dependency-groups]
dev = [
    "typedframes[mypy]>=0.2.1",
]
