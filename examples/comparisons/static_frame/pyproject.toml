[project]
name = "static_frame_examples"
version = "0.1.0"
description = "StaticFrame vs typedframes comparison examples"
requires-python = ">=3.11"
dependencies = [
    "mypy>=1.19.1",
    "static-frame>=5.1.1",
]

[dependency-groups]
dev = [
    "pandas>=3.0.1",
    "typedframes[mypy]>=0.2.1",
    "pandas-stubs>=3.0.0",
]

[tool.typedframes]
enabled = true
